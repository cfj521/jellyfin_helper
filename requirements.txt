# Jellyfin Tools - Python 依赖（Web 后端 + tools/ 模块）
# 前端依赖见 web/frontend/package.json

# ── Web 框架 ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# ── 数据库 ──
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── HTTP / WebSocket ──
requests>=2.28.0
httpx>=0.26.0
aiohttp>=3.9.0
websockets>=12.0
# curl_cffi: 模拟 Chrome TLS/JA3 指纹，用于绕过 Cloudflare 防护（missav / fourhoi 图床等）
curl_cffi>=0.7.0

# ── 网页解析（字幕站点 / 成人内容刮削）──
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── 压缩包解压（字幕源常以 zip/rar/7z 打包）──
# zip / tar / tar.gz / tar.bz2 / tar.xz / gz 走 stdlib（zipfile / tarfile / gzip），无依赖
# 7z：py7zr 纯 Python 实现，零外部依赖
# rar：走系统 bsdtar 子进程（libarchive >= 3.6 支持 RAR5），无 PyPI 依赖
py7zr>=0.20.0

# ── 系统级工具（非 pip，安装方式见 README）──
# ffmpeg/ffprobe — 音轨扫描、字幕内嵌轨探测
# mkvtoolnix (mkvpropedit) — 修改 MKV 默认音轨
# bsdtar (libarchive >= 3.6) — 解压 rar / 7z 字幕包

# ── 认证 ──
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# ── 配置与工具 ──
PyYAML>=6.0
python-dateutil>=2.8.0
python-dotenv>=1.0.0
tqdm>=4.65.0
