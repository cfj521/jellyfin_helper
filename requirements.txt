# Jellyfin Tools - Python 依赖（Web 后端 + tools/ 模块）
# 前端依赖见 web/frontend/package.json

# ── Web 框架 ──
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# ── 数据库 ──
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── HTTP / WebSocket ──
requests>=2.28.0
httpx>=0.26.0
aiohttp>=3.9.0
websockets>=12.0

# ── 网页解析（字幕站点 / 成人内容刮削）──
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── 配置与工具 ──
PyYAML>=6.0
python-dateutil>=2.8.0
python-dotenv>=1.0.0
tqdm>=4.65.0
